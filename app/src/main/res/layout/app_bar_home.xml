<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".presentation.DoctorActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

    </android.support.design.widget.AppBarLayout>

    <include
        android:id="@+id/include_client_list_view"
        layout="@layout/activity_client_list"
        android:visibility="gone"/>

    <include
        android:id="@+id/include_appoint_view"
        layout="@layout/activity_calander"
        android:visibility="gone"/>

    <include
        android:id="@+id/include_notes_view"
        layout="@layout/activity_note"
        android:visibility="gone"/>

    <include
        android:id="@+id/include_medical_view"
        layout="@layout/activity_medical_terms"
        android:visibility="gone"/>

    <include
        android:id="@+id/include_edit_view"
        layout="@layout/activity_edit_client"
        android:visibility="gone"/>

    <include
        android:id="@+id/include_medlist_view"
        layout="@layout/activity_medication"
        android:visibility="gone"/>

</android.support.design.widget.CoordinatorLayout>
